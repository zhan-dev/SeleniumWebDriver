name: CI Pipeline

on:
  pull_request:
    branches: 
      - DEV
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:
    inputs:
      browser:
        description: 'Choosing browser for UI tests'
        required: true
        default: 'Chrome'
        type: choice
        options:
          - Chrome
          - Edge
          
jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run API tests
        run: dotnet test --no-build --configuration Release --logger "trx;LogFileName=api-tests.trx"

      - name: Upload API test artifacts
        uses: actions/upload-artifact@v3
        with:
          name: api-artifacts
          path: **/*.trx

  ui-tests:
    runs-on: ubuntu-latest
    needs: api-tests
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run UI tests
        run: dotnet test --no-build --configuration Release --logger "trx;LogFileName=ui-tests.trx"
        env:
          BROWSER: ${{ github.event.inputs.browser }}

      - name: Upload test artifacts
        uses: actions/upload-artifact@v3
        with:
          name: ui-artifacts
          path: |
            **/*.trx
            **/Screenshots/*